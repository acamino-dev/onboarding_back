{
  "name": "auth-register",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "unit": "jest",
    "lint": "eslint '*.ts' --quiet --fix",
    "compile": "tsc",
    "test": "npm run compile && npm run unit && npm run test:integration",
    "test:integration": "NODE_OPTIONS=--experimental-vm-modules jest --config jest.integration.config.ts",
    "seed:integration": "DB_SECRET_ID=onboardingCredentialsDev ts-node tests/integration/scripts/seed-integration.ts"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^22.0.0",
    "@types/pg": "^8.11.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3"
  }
}
